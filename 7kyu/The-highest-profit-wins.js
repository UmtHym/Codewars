function minMax(arr){
    return [Math.min(...arr), Math.max(...arr)]; 
  }
  
  //input = array of at least 1 element, never Null,undefined or similar,
  //return = min and max of the input array in array format,
  //use Math methods to find the min and max and return as a array with min
  //min as the first value and max as the second